"""
This Vercel/FastAPI starter does not include ML training.

If you meant the Diabetes Risk Prediction project, use that folder instead:

  cd path\\to\\Diabetes-Risk-Prediction-System-main
  python -m pip install -r requirements.txt
  python backend_model.py

On Windows, if `pip` fails with "Fatal error in launcher" / Python311 not found,
use: python -m pip install ... (uses your current Python, e.g. 3.13).
"""

import sys

if __name__ == "__main__":
    print(__doc__)
    sys.exit(1)
